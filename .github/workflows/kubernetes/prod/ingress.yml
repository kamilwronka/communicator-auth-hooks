apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: communicator-auth-hooks-service
  namespace: communicator-prod
  annotations:
    konghq.com/protocols: https
    konghq.com/https-redirect-status-code: '308'
    konghq.com/plugins: communicator-prod-api-key
spec:
  tls:
    - hosts:
        - prod.auth-hooks-service.communicator.kamilwronka.com
      secretName: communicator-prod-tls
  ingressClassName: kong
  rules:
    - host: prod.auth-hooks-service.communicator.kamilwronka.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: communicator-auth-hooks-service
                port:
                  number: 4000
