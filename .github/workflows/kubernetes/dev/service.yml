apiVersion: v1
kind: Service
metadata:
  name: communicator-auth-hooks-service
  labels:
    app: communicator-auth-hooks-service
  namespace: communicator-dev
spec:
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: communicator-auth-hooks-service